package Token;

public class TokenMap {
    public String tokenName;
    public Init.tokenType tokenType;

    public TokenMap(String tokenName, Init.tokenType tokenType) {
        this.tokenName = tokenName;
        this.tokenType = tokenType;
    }
}
